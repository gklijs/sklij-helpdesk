# Dev server port matches dex/config.yaml's own registered redirect URI
# (http://127.0.0.1:8081/callback) for the "skilj-helpdesk-frontend"
# static client - changing this needs a matching change there too.
[serve]
address = "127.0.0.1"
port = 8081

[build]
target = "index.html"
